import { RowToggleDetailDirective } from './row-toggle-detail.directive';

describe('RowToggleDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new RowToggleDetailDirective();
    expect(directive).toBeTruthy();
  });
});
